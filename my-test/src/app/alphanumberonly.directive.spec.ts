import { AlphanumberonlyDirective } from './alphanumberonly.directive';

describe('AlphanumberonlyDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphanumberonlyDirective();
    expect(directive).toBeTruthy();
  });
});
